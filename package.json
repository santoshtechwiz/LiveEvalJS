{
  "name": "quokka-lite",
  "displayName": "Quokka Lite - Inline JS Evaluator",
  "publisher": "your-name",
  "engines": { "vscode": "^1.60.0" },
  "version": "0.0.1",
  "description": "Lightweight Quokka-like extension that evaluates marked JS lines inline, shows results, and provides simple coverage visualization.",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-name/quokka-lite"
  },
  "license": "MIT",
  "categories": ["Other"],
  "activationEvents": [
    "*",
    "onCommand:quakka-js-clone.evaluateCurrentFile",
    "onCommand:quakka-js-clone.toggleEvaluation",
    "onCommand:quakka-js-clone.clearResults"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      { "command": "quakka-js-clone.evaluateCurrentFile", "title": "Quakka.js Clone: Evaluate Current File" },
      { "command": "quakka-js-clone.toggleEvaluation", "title": "Quakka.js Clone: Toggle Evaluation" },
      { "command": "quakka-js-clone.clearResults", "title": "Quakka.js Clone: Clear Results" }
    ],
    "configuration": {
      "type": "object",
      "title": "Quokka-like settings",
      "properties": {
        "quakka-js-clone.resultColor": { "type": "string", "default": "#888888", "description": "(legacy) Color used to render evaluation results" },
        "quakka-js-clone.errorColor": { "type": "string", "default": "#ff6b6b", "description": "(legacy) Color used to render evaluation errors" },
        "quakka-js-clone.autoEvaluate": { "type": "boolean", "default": true, "description": "(legacy) Automatically evaluate and annotate files when opening/changing" },
        "quakka-js-clone.evaluationDelay": { "type": "number", "default": 500, "description": "(legacy) Delay (ms) after edits before evaluation runs" },
        "quakka-js-clone.maxLineLength": { "type": "number", "default": 100, "description": "(legacy) Skip lines longer than this when evaluating" },
        "quakka-js-clone.showErrors": { "type": "boolean", "default": true, "description": "(legacy) Show errors inline for failed evaluations" },

        "quokkaJsClone.resultColor": { "type": "string", "default": "#7a7a7a", "description": "Color used to render evaluation results" },
        "quokkaJsClone.errorColor": { "type": "string", "default": "#ff4d4f", "description": "Color used to render evaluation errors" },
        "quokkaJsClone.autoEvaluate": { "type": "boolean", "default": true, "description": "Automatically evaluate and annotate files when opening/changing" },
        "quokkaJsClone.evaluationDelay": { "type": "number", "default": 400, "description": "Delay (ms) after edits before evaluation runs" },
        "quokkaJsClone.maxEvaluationsPerFile": { "type": "number", "default": 200, "description": "Maximum evaluations per file pass" },
        "quokkaJsClone.timeoutPerRunMs": { "type": "number", "default": 600, "description": "Timeout per vm run (ms)" },
        "quokkaJsClone.showErrors": { "type": "boolean", "default": true, "description": "Show errors inline for failed evaluations" }
      }
    },
    "views": {
      "explorer": [
        { "id": "quokkaResults", "name": "Quokka-like Results", "icon": "resources/icon.png" }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "pretest": "npm run compile",
    "test": "jest --colors"
  },
  "dependencies": { "isolated-vm": "^6.0.1", "vm2": "^3.9.19", "acorn": "^8.8.2" },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/vscode": "^1.103.0",
    "typescript": "^5.9.2",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}
